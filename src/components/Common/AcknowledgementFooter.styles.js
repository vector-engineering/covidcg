import styled from 'styled-components';

export const AcknowledgementFooterContainer = styled.div`
  display: block;
  font-weight: normal;

  background-color: #f8f8f8;
  border-top: 1px solid #aaa;
  padding: 1rem;
  padding-top: 0.5rem;
`;

export const SectionTitle = styled.span`
  font-size: 0.9rem;
  font-weight: bold;
  margin-top: 0.5rem;
`;

export const ContentText = styled.div`
  p {
    margin: 0.3rem 0;
    font-size: 0.75rem;
    line-height: normal;
    color: rgba(0, 0, 0, 0.6);
  }
`;
